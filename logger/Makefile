
-include $(TOPDIR)/Make.defs
-include $(SDKDIR)/Make.defs

CONFIG_GNSSLOGGER_LOGGER_PRIORITY ?= SCHED_PRIORITY_DEFAULT
CONFIG_GNSSLOGGER_LOGGER_STACKSIZE ?= 2048

APPNAME = logger
PRIORITY = $(CONFIG_GNSSLOGGER_LOGGER_PRIORITY)
STACKSIZE = $(CONFIG_GNSSLOGGER_LOGGER_STACKSIZE)

ASRCS =
CSRCS =
MAINSRC = logger_main.c

CONFIG_GNSSLOGGER_LOGGER_PROGNAME ?= logger$(EXEEXT)
PROGNAME = $(CONFIG_GNSSLOGGER_LOGGER_PROGNAME)

include $(APPDIR)/Application.mk
