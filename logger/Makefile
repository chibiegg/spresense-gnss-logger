-include $(TOPDIR)/Make.defs
-include $(SDKDIR)/Make.defs

CONFIG_SPRESENSE-GNSS-LOGGER_LOGGER_PRIORITY ?= SCHED_PRIORITY_DEFAULT
CONFIG_SPRESENSE-GNSS-LOGGER_LOGGER_STACKSIZE ?= 2048

APPNAME = logger
PRIORITY = $(CONFIG_SPRESENSE-GNSS-LOGGER_LOGGER_PRIORITY)
STACKSIZE = $(CONFIG_SPRESENSE-GNSS-LOGGER_LOGGER_STACKSIZE)

ASRCS =
CSRCS = $(wildcard *.c) $(wildcard */*.c)
CXXSRCS = $(wildcard *.cpp) $(wildcard */*.cpp) $(wildcard *.cxx) $(wildcard */*.cxx)
MAINSRC = logger_main.c

CONFIG_SPRESENSE-GNSS-LOGGER_LOGGER_PROGNAME ?= logger$(EXEEXT)
PROGNAME = $(CONFIG_SPRESENSE-GNSS-LOGGER_LOGGER_PROGNAME)

include $(APPDIR)/Application.mk
